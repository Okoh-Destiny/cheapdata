{
  "name": "cheapdata-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "CheapData monorepo",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "start": "npm run start --workspace=@cheapdata/api",
    "dev": "npm run dev --workspace=@cheapdata/api",
    "api": "npm run start --workspace=@cheapdata/api",
    "db:fix": "npm run db:fix --workspace=@cheapdata/api",
    "db:reset-fields": "npm run db:reset-fields --workspace=@cheapdata/api",
    "db:purchase-pin": "npm run db:purchase-pin --workspace=@cheapdata/api",
    "make-admin": "npm run make-admin --workspace=@cheapdata/api"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.20.0",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}